import ResultsScreen from "./ResultsScreen";
import { render } from "test-utils";

test("it should display one card for each album that is found", () => {
  throw new Error();
});

test("it should display a message at the top", () => {
  throw new Error();
});
